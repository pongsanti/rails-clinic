table.table.table-striped.table-hover
  thead
    tr
      th ID
      th Name
      th Note
      th
  tbody.rowlink data-link="row"
    - exam.patient_diags.each do |pd|
      tr id=("new_created" if pd.id == @patient_diags_id)
        td = pd.id 
        td = pd.diag.name
        td = pd.note
        td.rowlink-skip
          a href=edit_patient_diag_path(pd) data-remote="true" data-disable-with='<span class="glyphicon glyphicon-time"></span>'
            span class="glyphicon glyphicon-edit"
