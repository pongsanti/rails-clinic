<%= render partial: "navigation", locals: {active: :index} %>
<h3><%= t '.title' %> <small><%= t '.sub_text' %></small></h3>
<hr>

<div class="table-responsive">
  <table class="table table-striped table-hover">
    <thead>
      <tr>
        <th><%= sort_link @q, :id, "#" %></th>
        <th><%= sort_link @q, :name %></th>
        <th><%= sort_link @q, :description %></th>
        <th></th>
      </tr>
    </thead>
    <tbody data-link="row" class="rowlink">
      <% @diags.each do |diag| %>
      <tr>
        <td><%= diag.id %></td>
        <td><%= diag.name %></td>
        <td><%= diag.description %></td>
        <td class="rowlink-skip">
          <div class="btn-group">
            <%= link_to diag_path(diag), class: 'btn btn-default btn-sm', role: 'button' do %>
              <span class="glyphicon glyphicon-eye-open" data-toggle="tooltip" title="View" data-placement="bottom" aria-hidden="true"></span>
            <% end %>
            <%= link_to edit_diag_path(diag), class: 'btn btn-default btn-sm', role: 'button' do %>
              <span class="glyphicon glyphicon-edit" data-toggle="tooltip" title="Edit" data-placement="bottom" aria-hidden="true"></span>
            <% end %>
          </div>
        </td>
      </tr>
      <% end %>
    </tbody>
  </table>
  <%= paginate @diags %>
</div>
<hr>
<div>
<%= page_entries_info @diags, entry_name: t('.title') %>
</div>