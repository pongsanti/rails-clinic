<%= form_for(@diag) do |f| %>
  <% if @diag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@diag.errors.count, "error") %> prohibited this diag from being saved:</h2>

      <ul>
      <% @diag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
